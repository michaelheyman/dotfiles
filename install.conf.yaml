- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.config/fish: fish
    ~/.local/bin: bin
    ~/.shell: shell
    ~/.bash_profile:
    ~/.bashrc:
      force: true
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.zshrc:
      force: true

- shell:
  - [git submodule update --init --recursive, Installing submodules]
